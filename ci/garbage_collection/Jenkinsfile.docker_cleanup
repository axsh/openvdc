#!groovy


def cleanup() {
  node("master") {
    stage "Docker Cleanup"
    checkout scm
    sh "cd ci/garbage_collection/docker_cleanups/ ; ./docker_garbage_collection.sh"
  }
}

node() {
    stage "Checkout"
}

cleanup()

