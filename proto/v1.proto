syntax = "proto3";

package api;

service Instance {
	rpc Run (RunRequest) returns (RunReply){}
}

message RunRequest{
	string imageName = 1;
	string hostName = 2;
	string taskType = 3;
}
message RunReply {
  string instance_id = 1;
}

