syntax = "proto3";

package model;

message Instance {
  string id = 1;
  string executor_id = 2;
}

enum ResourceType {
  VM = 0;
}

enum ResourceState {
  Registed = 0;
  Unregistered = 1;
}

message Resource {
  string id = 1;
  ResourceType type = 2;
  ResourceState state = 3;
}
